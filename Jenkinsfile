pipeline {
agent any
stages {
       stage("build") {
       steps{
       echo "buiiling ankit first pipeline"
       }
       }
       stage("test") {
       steps {
       echo "testing   ankit first pipeline"
       }
       }
       stage("deploy") {
       steps {
       echo "deploy ankit first pipeline"
       }
       }
       }
       }
       
       
